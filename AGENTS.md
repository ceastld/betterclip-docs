# AGENTS — betterclip-docs

BetterClip 用户文档站（Docusaurus 3）。本机路径建议：`D:\source\repos\quicker\betterclip-docs`（`quicker-workspace` 兄弟目录）。

## 边界

| 做 | 不做 |
|----|------|
| 最终用户功能说明、更新记录 | 工程协议、启动流、类名当主线 |
| 推 `main` → GitHub Actions → Cloudflare Pages | `npm run deploy` / GitHub Pages 发生产 |
| 从产品 `DESIGN.md` / `release-notes/` **改写**上站 | 臆造未成文交互（默认消失方式、完整快捷键契约、云同步规则等） |
| changelog：**增量同步** release-notes（已有条目不覆盖） | 用脚本整份覆盖手工改写过的更新记录 |

产品设计权威：`quicker-workspace/clip/main/DESIGN.md`。未写明的产品决策先问用户。

## 本地命令

```powershell
npm ci
npm run start -- --host 127.0.0.1 --port 3000 --no-open
npm run build
npm run typecheck
npm run sync-changelog
```

改路径、链接、front matter 或配置后优先 `npm run build`。包管理只用 **npm** + `package-lock.json`。

## 站点约定

- `routeBasePath: '/'`，文档挂在站点根路径
- `blog: false`；更新历史用 `docs/changelog.md`
- 侧边栏 autogenerated；新 md 放进 `docs/` 即可
- `i18n` 默认 `zh-Hans`
- 生产 URL：`https://betterclip-docs.pages.dev`
- 界面示意：`src/components/*Preview`，在 `src/theme/MDXComponents.tsx` 与 `MDXContent` 注册；配色从 ClipHost `app.css` 切片，不发明色值

## Cloudflare Pages

| 项 | 值 |
|----|-----|
| 构建 | `npm run build` |
| 输出 | `build` |
| 生产分支 | `main` |
| Node | 22 |
| 部署 | `.github/workflows/deploy-pages.yml`（`CLOUDFLARE_API_TOKEN`） |

changelog 同步：`npm run sync-changelog` 或产品仓 `scripts/Sync-BetterClipDocsChangelog.ps1`。只插入缺失版本，不改写已有条目。

未经用户明确要求：不 commit、不 push。push `main` = 生产发布。

## 写作检查

- [ ] 行为与产品 DESIGN / 软件 UI 核对，未臆造
- [ ] 中文；UI 文案与软件一致
- [ ] 加粗写法：`**标签**：正文`（不要 `**标签：**正文`）
- [ ] 需要时已 `npm run build`
